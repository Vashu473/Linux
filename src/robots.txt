# Robots.txt file for tradelick.com

# Allow all robots full access except for specific directories
User-agent: *
Disallow: /admin/
Disallow: /private/
Disallow: /tmp/
Disallow: /cgi-bin/
Disallow: /example-page.html

# Allow full access to the rest of the site
Allow: /

# Sitemap locations
Sitemap: https://tradelick.com/sitemap.xml
Sitemap: https://tradelick.com/policies/sitemap.xml

# Crawling delay (optional)
# Crawl-delay: 10

# Redirect non-www to www
RewriteCond %{HTTP_HOST} ^tradelick\.com$ [NC]
RewriteRule ^(.*)$ https://www.tradelick.com/$1 [R=301,L]

# Uncomment to redirect www to non-www instead
# RewriteCond %{HTTP_HOST} ^www\.tradelick\.com$ [NC]
# RewriteRule ^(.*)$ https://tradelick.com/$1 [R=301,L]